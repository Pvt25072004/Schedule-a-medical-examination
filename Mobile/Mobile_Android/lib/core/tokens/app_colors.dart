import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF48A1F3); // Blue
  static const Color primaryDark = Color(0xFF143250); // Dark Blue
  static const Color primaryLight = Color(0xFFEBF5FF); // Light Blue

  static const Color secondary = Color(0xFF81C784); // Kept original secondary
  static const Color accent = Color(0xFFF99B1C); // Orange

  // Background Colors
  static const Color background = Color(0xFFF7F7F7); // Light gray background
  static const Color backgroundLight = Color(0xFFF5F7FA);
  static const Color surface = Colors.white;

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Status Colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFFFA000);

  // Border & Dividers
  static const Color divider = Color(0xFFBDBDBD);
  static const Color border = Color(0xFFE0E0E0);
}
