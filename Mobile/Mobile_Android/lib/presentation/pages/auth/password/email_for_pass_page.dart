import 'package:flutter/material.dart';

class EmailForPassPage extends StatelessWidget {
  const EmailForPassPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Quên mật khẩu')),
      body: const Center(child: Text('Chức năng đang được cập nhật.')),
    );
  }
}
